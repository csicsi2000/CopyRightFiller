{
  "Pattern": "*.cs",
  // Search all the files with the given pattern
  "SearchedFolder": "D:\\Concept\\CopyRigthFiller\\CopyRigthFiller\\TestTarget",
  // All the lines will be deleted with this beginning string, if the template is not the same
  // If the string is empty if will be ignored
  "DeleteLine": "//",
  // These files will be ignored during export
  "IgnoredFiles": [
    "Resource.Designer.cs"
  ],
  "LogFilepath" : "logs.txt" 
}
